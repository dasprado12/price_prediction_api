version: "3"
services: 
  app:
    build: .
    ports: 
      - "5000:5000"
    volumes: 
      - /tmp/app:/user/source/app/reports
    networks: 
      - development-network

networks:
  development-network:
    driver: bridge